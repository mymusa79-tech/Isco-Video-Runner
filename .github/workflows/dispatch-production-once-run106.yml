name: Retired Run 106 Dispatch Helper

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run 106 dispatch helper retired; canonical production was already dispatched."
