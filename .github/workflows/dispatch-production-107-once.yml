name: Dispatch Production 107 Once

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  inspect-only:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "One-shot dispatcher already used; no automatic dispatch remains."
