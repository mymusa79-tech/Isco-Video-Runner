name: Verify Production Stage Ladder

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  bootstrap-presence:
    runs-on: ubuntu-24.04
    steps:
      - name: Bootstrap workflow registration only
        run: echo "Stage Ladder workflow is registered on main; full P0-P6 implementation is supplied by the Stage Ladder PR."
