# Run212 Visual Candidate Utilization

Run212 closes three narrow gaps after global recovery fusion:

- stock query compaction preserves both stable head semantics and late beat semantics;
- severe semantic hard negatives are request-scoped unavailable across later Short beats;
- Short Cinematic uses PASS=STOP with at most two primary and two fused-recovery Vision verdicts per added beat.

Long-form keeps its existing adaptive Opening/Section review caps. All final Vision, Security, Cultural/Islamic, advertiser and quality thresholds remain unchanged.
